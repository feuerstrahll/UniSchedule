name: furshed
description: Flutter project working with API ruz.fa.ru.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^5.7.0
  go_router: ^8.2.0
  intl: ^0.20.2
  time_scheduler_table:
    path: packages/time_scheduler_table/


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
